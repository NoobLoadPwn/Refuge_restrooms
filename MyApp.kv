<MyGridLayout>:

    label:mylabel
    cols:2
    spacing:10

    Label:
        text:"ADA"
        font_size:30

    CheckBox:
        on_active:root.checkbox_click(self, self.active)

    Label:
        text:"Unisex"
        font_size:30

    CheckBox:
        on_active:root.checkbox_click(self, self.active)

    Label:
        text:"Changing table"
        font_size:30

    CheckBox:
        on_active:root.checkbox_click(self, self.active)

    Label:
        id:mylabel
        text:"Label"
        font_size:20
